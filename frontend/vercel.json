{
  "installCommand": "if [ -f package-lock.json ]; then npm ci; else npm ci --prefix frontend; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else npm run build --prefix frontend; fi"
}